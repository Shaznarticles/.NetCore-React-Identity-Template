import React from 'react';

const Logout = props => {

    return (
        <>
            <header>
                <h1>Log out</h1>
                <p>You have successfully logged out of the application.</p>
            </header>
        </>
    );
};

export default Logout;